name: Dataset compliance

on:
  workflow_call:
    inputs:
      dotnet_version:
        type: string
        required: true
      configuration:
        type: string
        required: true
    secrets:
      GH_TOKEN:
        required: false  # optional PAT for private deps; else github.token
      BHOM_APP_ID:
        required: false  # BHoMBot GitHub App ID — used to mint a short-lived token for dep cloning.
      BHOM_APP_PRIVATE_KEY:
        required: false  # BHoMBot GitHub App private key (PEM). Required when BHOM_APP_ID is set.

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}-dataset
  cancel-in-progress: true

jobs:
  dataset:
    runs-on: windows-latest
    timeout-minutes: 20
    permissions:
      contents: read
      pull-requests: read
      actions: write
    defaults:
      run:
        shell: pwsh

    steps:
      - name: Checkout caller repo
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          fetch-depth: 1

      - name: Compute changed dataset files
        id: changed
        run: |
          if ("${{ github.event_name }}" -ne "pull_request") {
            "count=0" | Out-File -FilePath $env:GITHUB_OUTPUT -Append
            Write-Host "::notice::Not a pull request — dataset check skipped."
            exit 0
          }

          $baseSha = "${{ github.event.pull_request.base.sha }}"
          git fetch --depth=1 origin $baseSha

          $files = git diff --name-only --diff-filter=ACMRT "$baseSha" HEAD |
                   Where-Object {
                     $_.ToLower().EndsWith(".json") -and
                     ($_.Replace('\', '/') -match '(?i)datasets')
                   }

          $count = ($files | Measure-Object).Count
          $files -join "`n" | Out-File changed_dataset_files.txt -Encoding utf8

          "count=$count" | Out-File -FilePath $env:GITHUB_OUTPUT -Append

          if ($count -gt 0) {
            Write-Host "::notice::$count dataset file(s) changed — running dataset compliance."
            Get-Content changed_dataset_files.txt
          } else {
            Write-Host "::notice::No changed dataset files detected — skipping dataset compliance."
          }

      - name: Check App credentials
        id: app-check
        if: steps.changed.outputs.count != '0'
        shell: bash
        env:
          APP_ID: ${{ secrets.BHOM_APP_ID }}
        run: |
          if [ -n "$APP_ID" ]; then
            echo "available=true" >> "$GITHUB_OUTPUT"
          else
            echo "available=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Generate dependency token
        id: dep-token
        if: steps.changed.outputs.count != '0' && steps.app-check.outputs.available == 'true'
        uses: actions/create-github-app-token@f8d387b68d61c58ab83c6c016672934102569859 # v3.0.0
        with:
          app-id: ${{ secrets.BHOM_APP_ID }}
          private-key: ${{ secrets.BHOM_APP_PRIVATE_KEY }}
          owner: ${{ github.repository_owner }}

      - name: Resolve dependencies (Test_Toolkit graph)
        if: steps.changed.outputs.count != '0'
        uses: BuroHappoldEngineeringSandbox/DevOps_Toolkit/.github/actions/resolve-dependencies@main
        with:
          mode:           seeds
          seeds:          BHoM/Test_Toolkit
          dotnet_version: ${{ inputs.dotnet_version }}
          configuration:  ${{ inputs.configuration }}
          token:          ${{ steps.dep-token.outputs.token || secrets.GH_TOKEN || github.token }}

      - name: Prepare DatasetComplianceRunner
        if: steps.changed.outputs.count != '0'
        id: runner
        uses: BuroHappoldEngineeringSandbox/DevOps_Toolkit/.github/actions/prepare-runner@main
        with:
          runner_project: tools/ComplianceRunner/src/DatasetComplianceRunner/DatasetComplianceRunner.csproj
          output_dir:     out\dataset-runner
          configuration:  ${{ inputs.configuration }}
          cache_prefix:   dataset-runner
          dotnet_version: "10.x"  # runner targets net10.0 — independent of the dep-build SDK (8.0)

      - name: Run dataset compliance
        id: run_checks
        if: steps.changed.outputs.count != '0'
        run: |
          $runner = "${{ steps.runner.outputs.runner_exe }}"

          $fileList = @(Get-Content changed_dataset_files.txt)

          Write-Host "::group::BHoM dataset compliance"
          & $runner --output github @fileList
          $ok = ($LASTEXITCODE -eq 0)
          Write-Host "::endgroup::"

          if ($env:GITHUB_STEP_SUMMARY) {
            $icon   = if ($ok) { ":white_check_mark:" } else { ":x:" }
            $status = if ($ok) { "Passed" } else { "**Failed**" }
            @(
              "### Dataset compliance check results",
              "",
              "| Check | Result |",
              "|---|---|",
              "| dataset | $icon $status |"
            ) | Out-File -FilePath $env:GITHUB_STEP_SUMMARY -Encoding utf8 -Append
          }

          if (-not $ok) {
            Write-Host "::error::Dataset compliance check failed."
            exit 1
          }
