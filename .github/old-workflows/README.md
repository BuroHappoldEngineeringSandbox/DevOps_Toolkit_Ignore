# Archived workflows

These files are **not** active. GitHub Actions only loads workflows from `.github/workflows/`.

They are kept for reference while migrating to `ci-orchestrator.yml` and related reusable workflows.
